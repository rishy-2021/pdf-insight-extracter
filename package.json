{
  "name": "pdf-insight-extracter",
  "version": "1.0.0",
  "description": "Upload Pdf and get insights from it",
  "scripts": {
    "install:client": "cd client && npm install",
    "install:server": "cd server/node && npm install",
    "install:all": "npm run install:client && npm run install:server",
    "start:client": "cd client && npm start",
    "start:server": "cd server/node && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
